# Frontend Environment Variables
VITE_API_URL=https://your-functions-app.azurewebsites.net
VITE_ENVIRONMENT=production

# Note: These variables are automatically set during Azure deployment
# via the postprovision hook in azure.yaml